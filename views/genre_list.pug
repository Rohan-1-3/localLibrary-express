extends layout

block content
  h1= title

  if allGenreList.length
    ul
        each genre in  allGenreList
            li
                a(href = genre.url)  #{genre.name}
  else
    p There are no genres.